./setcname.sh ${SERVICE}-master.${ENVID}.coscale.com ${HOSTNAME}.${ENVID}.coscale.com
#this one is because the migrations script can take only "postgresql" as hostname
./setcname.sh ${SERVICE}.${ENVID}.coscale.com ${HOSTNAME}.${ENVID}.coscale.com

